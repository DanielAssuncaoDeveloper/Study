function setup() {
  createCanvas(window.innerWidth, window.innerHeight);
  frameRate(2);
}

function draw() {
  // if (mouseY <= width/2) {
  //   background(random(0,255), random(0, 255), random(0, 255));
  // }
}